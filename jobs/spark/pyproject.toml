[project]
name = "spark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Naineel Soyantar", email = "soyantarnaineel@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "delta-spark==3.2.0",
    "openpyxl>=3.1.0",
    "pandas>=2.0.0",
    "pyspark==3.5.1",
]

[project.scripts]
spark = "spark:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
